node_modules
dist

# Rust / Cargo
target
**/target
# Cargo.lock IS committed for the contracts workspace — it pins dependency
# resolution so CI (cargo test --locked) builds reproducibly.

# Local environment files
.env
.env.*
!.env.example

# macOS / editor noise
.DS_Store
.vscode

pnpm-workspace.yaml
pnpm-lock.yaml
